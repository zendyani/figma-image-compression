import Image from "../../shared/entities/Image";

const downloadAll = (images: Image[]) => {
    /**
     * - Check Image information
     * - Send to compression service
     * - Add the result to the zip file
     * - Force the download operation
     */
}